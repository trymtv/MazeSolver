package maze;

import java.io.File;
import java.util.LinkedList;

public class MazeMaker {
	private static LinkedList<int[]> queue = new LinkedList<>();
	
	private static void make(String targetPath, int size) {
		File outputFile = new File(targetPath);
	}
}
